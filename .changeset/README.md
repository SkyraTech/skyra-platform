# Changesets

This directory contains your changesets, which are markdown files that contain the summary of the changes you've made to your packages.

When you want to add a new changeset, run `pnpm changeset`.

When it is time to release, run `pnpm changeset version`.
