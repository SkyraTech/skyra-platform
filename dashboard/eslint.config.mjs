// ESLint flat config for @skyra/platform dashboard
// Minimal config: type-safety is enforced by TypeScript (tsc --noEmit)

export default [
  {
    ignores: ['.next/**', 'node_modules/**'],
  },
];
